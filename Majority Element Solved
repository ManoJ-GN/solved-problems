class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        dic={}
        for i in nums:
            if i in dic:
                s=dic[i]
                dic[i]=s+1
            else:
                dic[i]=1
        s=max(dic.values())
        for k in dic:
            if dic[k]==s:
               return k


"""
Example 1:

Input: nums = [3,2,3]
Output: 3
Example 2:

Input: nums = [2,2,1,1,1,2,2]
Output: 2
"""
